// Clean sample file for testing

export function cleanFunction(): number {
  const value = 42;
  return value;
}

export function anotherCleanFunction(input: string): string {
  return input.toUpperCase();
}
